name: log_reporter

on:
  pull_request:
    paths:
      - 'layer/log_reporter/**'
      - '.github/workflows/log_reporter.yaml'

jobs:
  analyze_and_test:
    name: Analyze & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.1

      - name: Install dependencies
        run: flutter pub get

      - name: Verify formatting
        working-directory: layer/log_reporter
        run: dart format --set-exit-if-changed .

      - name: Static analysis
        working-directory: layer/log_reporter
        run: dart analyze --fatal-infos .

      - name: Run tests
        working-directory: layer/log_reporter
        run: flutter test

