name: firebase

on:
  pull_request:
    paths:
      - 'layer/firebase/**'
      - '.github/workflows/firebase.yaml'
      - '.github/actions/**'

jobs:
  analyze_and_test:
    name: Analyze & Test
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Flutter Module
        uses: ./.github/actions/setup-flutter-module
        with:
          module-path: layer/firebase
      
      - name: Quality Checks
        uses: ./.github/actions/flutter-quality-checks
        with:
          module-path: layer/firebase
          collect-coverage: 'true'
          upload-coverage: 'true'
